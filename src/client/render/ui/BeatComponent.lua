local Roact = require(game.ReplicatedStorage.Common.Roact)

local SIZE = 50 --50 pixels squared.

local BeatComponent = Roact.Component:extend("BeatComponent")

function BeatComponent:render()
    return Roact.createElement("TextLabel", {
		
    })
end